/* components/common/img-blur/img-blur.wxss */

/* 阻止背景超出 */
.stop-overflow {
  overflow: hidden;
  position: fixed;
  z-index: -1;
}

/* 模糊背景图片 */
.blur-bg {
  position: absolute;
  top: 0rpx;
  width: 100%;
  height: 100%;
  z-index: -1;
  filter: blur(40rpx);
  transform: scale(1.5)
}
